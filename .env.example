# REST API (whatsapp-conversation-api)
VITE_API_BASE_URL=http://localhost:3001/api

# When true, uses in-memory frontend mocks instead of the API
VITE_USE_MOCK_DATA=false

# Pusher simulator WebSocket (enabled by default)
VITE_REALTIME_WS_URL=ws://localhost:3001/ws
VITE_REALTIME_ENABLED=true
